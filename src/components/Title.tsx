import { Typography } from "@mui/material";

function Title() {
  return (
    <Typography
      color="primary"
      style={{
        width: "90vw",
        textAlign: "left",
        fontSize: "2rem",
        fontWeight: 400,
        margin: "17px auto",
        padding: "0 10px",
      }}
    >
      Job Portal
    </Typography>
  );
}

export default Title;
